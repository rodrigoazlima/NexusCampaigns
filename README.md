# NexusCampaigns — Pathway DM Knowledge Factory

AI-powered pipeline that transforms raw campaign inspiration (images, documents, notes) into reusable, linked, quality-gated knowledge assets for Dungeon Masters.

**Vault root:** `knowledge-base/`  
**Version:** 1.0 · **Date:** 2026-06-09

> Every agent reads `AGENTS.md` before making changes. It is the operating system of this vault.

---

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Windows Service (NSSM)                       │
│  daemon.ps1 → runner.ps1 (every 60s)                                │
│              ↓                                                       │
│   tasks.json + tasks-state.json → dispatch scheduled agents         │
└─────────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         Agent Pipeline                              │
│                                                                     │
│  00-Inbox/  →  [Ingestion] → inbox-queue.json                       │
│                    ↓                                                │
│             [Vision Agent] → 01-Processing/ drafts                  │
│                    ↓                                                │
│             [Lore Agent]   → NPC sheets (01-Processing/)            │
│                    ↓                                                │
│             [Token Agent]  → circular tokens (00-Inbox/images/)     │
│                    ↓                                                │
│             [Classification Agent] → enriched frontmatter           │
│                    ↓                                                │
│             (human review — sets status: approved, quality: N)      │
│                    ↓                                                │
│               02-Library/ (canon)                                   │
│                    ↓                                                │
│             [Wikilink Agent] → [[links]] in 02-Library/             │
│             [Wiki Agent]     → 03-Campaigns/, 04-Relationships/     │
└─────────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      Health & Observability                         │
│  [Review Agent]  → .system/reports/report-YYYY-MM-DD.json          │
│  [Repair Agent]  → .system/reports/repair-YYYY-MM-DD.json          │
│  [Cleanup Agent] → purge logs/reports older than cleanupDays        │
│  agent-metrics.json → per-agent run history (last 100 runs)         │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Folder Structure

| Folder | Purpose | Rules |
|--------|---------|-------|
| `00-Inbox/` | Raw source material drop zone | Read-only by agents. Ingestion Agent may rename files (emoji cleanup) only. |
| `00-Inbox/images/` | Campaign art organized by arc (`A1`, `A2`, `A9 Desert`, etc.) | Agents rename to slug format; never delete. |
| `00-Inbox/docs/` | World-building documents, PDFs | Read-only; DOCX → GFM Markdown via pandoc. |
| `00-Inbox/songs/` | Ambient music references | Read-only. |
| `00-Inbox/tokens/` | Token frames / molduras | Read-only. |
| `01-Processing/` | AI-generated drafts pending human review | Agents write here. Single source of truth for unreviewed entities. |
| `02-Library/` | Approved canon knowledge | Human-only promotion. Agents must never write here. |
| `03-Campaigns/` | Campaign-specific material | References Library entities via `[[wikilink]]`. No duplicates. |
| `04-Relationships/` | Auto-generated knowledge graphs | Regenerated by relationship agents. Do not edit manually. |
| `05-Assets/` | Approved media (portraits, tokens, maps) | Every asset must link to ≥1 Library entity. |
| `99-Archive/` | Retired approved content | Never permanently delete. |
| `.system/` | Automation scripts, state, indices, logs, reports | Agent workspace. |

---

## Pipeline Workflow

```
00-Inbox  →  01-Processing  →  (human review)  →  02-Library
                                                        ↓
                                                   03-Campaigns
                                                   04-Relationships
                                                   05-Assets
```

1. Drop sources into `00-Inbox/` — treat as read-only
2. Automation agents process `00-Inbox/` → write drafts to `01-Processing/`
3. Human reviews drafts, sets `status: approved` + `quality: N`
4. Approved content promoted to `02-Library/` (or `03-Campaigns/`, `05-Assets/`)
5. Relationships auto-indexed into `04-Relationships/`

**Quality gate for `02-Library/`:** `status: approved` + `quality >= 7` + `reviewed: true`  
No agent may write these fields as `true` / `approved`.

---

## Agents

| Agent | Script | Trigger | Role |
|-------|--------|---------|------|
| Ingestion | `11-ingestion-agent.ps1` | hourly | Emoji cleanup, DOCX→MD conversion, queue registration |
| Vision | `06-classify-images.ps1` | hourly | Image classification via Qwen3-VL; writes `01-Processing/` drafts |
| Lore | `09-generate-npcs.ps1` + `.py` | hourly | NPC sheet generation from image + scenario context |
| Token | `10-generate-tokens.ps1` + `.py` | hourly | Circular token generation with moldura frame |
| Classification | `04-enrich-tags.ps1` | hourly | Tag enrichment and type inference for sparse notes |
| Wiki | `03-compile-wiki.ps1` | hourly | Synthesizes entity pages from `00-Inbox/` markdown |
| Wikilink | `14-wikilink-library.py` | hourly | Inserts `[[wikilinks]]` into `## Related` sections of `02-Library/` |
| Review | `08-daily-report.ps1` | every 15 min | Pending review list, orphan detection, quality suggestions, log summary |
| Repair | `12-repair-agent.ps1` | every 15 min | Fixes stale locks, missing dirs, broken image refs; detects overdue agents |
| Cleanup | `13-cleanup.ps1` | daily | Purges logs/reports older than `cleanupDays` (default: 90) |

### LLM Integration

| Agent | Endpoint | Model | Purpose |
|-------|----------|-------|---------|
| Vision | `http://localhost:1234` | `qwen3-vl-4b-instruct` | Image classification |
| Lore | `http://localhost:1234` | `qwen3-vl-4b-instruct` | NPC generation |
| Classification | `http://localhost:8080` | unspecified | Tag enrichment |

All LLM calls: `temperature: 0`, up to 3 retries with 3s backoff. Connection errors → skip batch, retry next run.

---

## Automation System

**Process model:** NSSM Windows Service → `daemon.ps1` → `runner.ps1` (every 60s) → dispatches tasks from `tasks.json`.

**Default intervals:**

| Interval | Tasks |
|----------|-------|
| 900s (15 min) | `review-agent`, `repair-agent` |
| 3600s (1 h) | `ingestion-agent`, `vision-agent`, `lore-agent`, `token-agent`, `classification-agent`, `wiki-agent`, `wikilink-agent` |
| 86400s (24 h) | `cleanup-agent` |

**Adding a new agent:**

1. Create `.system/NN-<name>.ps1` or `.py` following existing conventions
2. Emit `--- START ---` and `--- DONE (key: N, elapsed: Ns) ---` log markers
3. Add to `.system/tasks.json`: `{ "id": "...", "script": "NN-name.ps1", "intervalSeconds": 3600, "description": "..." }`
4. Add to `.system/tasks-state.json`: `{ "agent-id": { "lastRun": "1970-01-01T00:00:00.0000000-00:00" } }`

**Git integration:** after every successful run, `runner.ps1` runs `git add -A && git commit && git push` (skipped if no changes).

---

## Metadata Standard

```yaml
---
id: npc-necromancer-black-hollow
type: npc
status: approved
quality: 8
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags:
  - undead
  - villain
source:
  - necromancer_001.png
reviewed: true
relationships:
  - "[[location-black-hollow-cemetery]]"
  - "[[faction-cult-of-undying]]"
---
```

**Allowed `type` values:**  
`npc` · `character` · `faction` · `location` · `city` · `village` · `dungeon` · `item` · `artifact` · `quest` · `encounter` · `creature` · `monster` · `event` · `religion` · `organization` · `timeline` · `lore`

**Quality scores:**

| Score | Meaning |
|-------|---------|
| 1–3 | Low quality — reject |
| 4–6 | Needs review |
| 7–8 | Good |
| 9–10 | Library candidate |

---

## Naming Convention

Slug format: `{type}-{descriptors}.md`

```
npc-necromancer-black-hollow.md
quest-missing-villagers.md
location-black-hollow-cemetery.md
battlemap-dungeon-cirit-01.md
```

Forbidden: `final_v2`, `new`, `cool`, `Untitled`, uppercase, spaces (except arc folder names like `A9 Desert`).

---

## Wikilinks & Linking Rules

Use `[[slug-name]]` — match exact filename without extension. Every entity must link to ≥1 other. No orphans.

| Entity | Required links |
|--------|---------------|
| npc | location, faction, or quest |
| quest | NPC + location |
| location | NPC or faction |
| faction | NPC + location |
| item | quest or owner NPC |
| encounter | location + creature |

---

## Logging & Encoding

**Log format:** `[YYYY-MM-DD HH:mm:ss] [<task-id>] <message>`

| File | Content |
|------|---------|
| `.system/logs/automation.log` | Consolidated all-agent log |
| `.system/logs/<script-basename>_YYYY-MM-DD.log` | Per-script daily rotation |

Every script emits `--- START ---` and `--- DONE ---`. `--- DONE ---` format:  
`--- DONE (classified: N, failed: N, elapsed: N.Ns) ---`

**Encoding:** All file I/O must use `-Encoding UTF8` (PowerShell) or `encoding='utf-8'` (Python). BOM stripping required when reading `agent-metrics.json`.

---

## Security Constraints

- No agent may self-approve content (`reviewed: true` — human-only)
- No agent may write to `02-Library/` without `reviewed: true` already set by human
- No agent may delete files from `00-Inbox/`
- Breaking canon requires explicit human `git commit` with reason in message
- LLM endpoints are localhost-only; no external API keys required
