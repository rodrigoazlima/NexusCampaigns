"""vision.tools

Concrete modules:
  06_classify_images.py  — implements IImageClassifier + BaseAgent
  06_match_token.py      — implements IFaceMatcher
Dependencies: ILLMClient (vision_llm endpoint), IStateStore (processed-images.json)
"""
