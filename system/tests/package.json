{
  "name": "nexus-governance-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright governance tests for the agent runtime (repo invariants, not browser E2E).",
  "scripts": {
    "test": "playwright test",
    "test:unit": "playwright test unit",
    "test:e2e": "playwright test e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.1.0"
  }
}
