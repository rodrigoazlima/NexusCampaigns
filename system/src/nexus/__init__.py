"""Nexus Campaigns pipeline package.

`nexus.runner` — the scheduler (formerly agents/runtime/tools/runner.py).
`nexus.shared` — the shared agent library (formerly agents/shared).
`nexus.tasks` — static (no-LLM) pipeline tasks, one module per task script.

LLM-driven agents (vision, lore, classification, wiki) remain under agents/.
"""
